package com.mustafa.ederi.data.remote

/**
 * Retrofit endpoint definitions, added incrementally per spec §40 as each
 * backend feature (auth, accounts, transactions, ...) is approved and built.
 */
interface ApiService
